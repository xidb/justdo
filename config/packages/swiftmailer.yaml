swiftmailer:
    transport: smtp
    host: smtp.yandex.ru
    port: 465
    username: just.do-react
    encryption: ssl
    auth_mode: login
    password: '%env(MAILER_PASSWORD)%'
    spool: { type: 'memory' }